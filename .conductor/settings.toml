#:schema https://conductor.build/schemas/settings.repo.schema.json
"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
archive = "./scripts/worktree-down.sh --purge"
run = "npm run dev"
run_mode = "concurrent"
setup = "./scripts/worktree-up.sh"
